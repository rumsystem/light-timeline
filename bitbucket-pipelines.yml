definitions:
  services:
    docker:
      memory: 4096

pipelines:
  branches:
    circle:
      - step:
          name: build
          size: 2x
          services:
            - docker
          script:
            - ssh -tt huoju@103.61.37.242 "cd /data/deploy/build-server/build_shortcuts && git pull && ./12_build_circle.sh"
